FROM node:14

WORKDIR /usr/src/app

COPY . ./
RUN npm install

EXPOSE 8080

ARG TESTNAME
ARG TESTNAME1

ENV TESTNAME ${TESTNAME}
ENV TESTNAME1 ${TESTNAME1}
ENV HOST=0.0.0.0
ENV PORT=8080

RUN npm run build

CMD [ "npm", "run", "start" ]
